{
  "name": "bun-api-starter",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^5.21.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.21.1",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "prisma-gen": "npx prisma migrate dev --name init && npx prisma generate",
    "format-code": "prettier \"**/*.{js,ts,json,html,scss,spec.ts}\" --write"
  }
}
